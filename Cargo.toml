[package]
name = "ledger"
version = "0.1.0"
description = "When Excel isn’t dangerous enough"
edition = "2024"

[dependencies]
chrono = "0.4.41"
clap = { version = "4.5.40", features = ["derive"] }
pest = "2.8.1"
pest_derive = "2.8.1"
rust_decimal = { version = "1.37.2", features = ["macros"] }
